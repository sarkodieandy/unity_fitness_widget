<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unity_fitness_widget">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Minimum and target SDK versions are normally in build.gradle,
         but you can add uses-sdk if needed -->
    <!-- <uses-sdk android:minSdkVersion="19" android:targetSdkVersion="33" /> -->

    <application
        android:allowBackup="true"
        android:label="UnityFitnessWidget"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true">

        <!-- If you want to launch Unity in a separate activity, declare it here -->
        <!--
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:hardwareAccelerated="true">
        </activity>
        -->

    </application>

</manifest>
